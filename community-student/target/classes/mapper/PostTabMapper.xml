<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gx.community.mapper.PostTabMapper">
    <select id="getPostTabList" resultType="PostTab">
        select
        tabID,
        tabName
        from tb_post_tab
    </select>
</mapper>